package ch.frickler.jass.db.enums;

/**
 * this enum stores card family types, of witch a card consist.
 * if changeing the enum you have to change the translation witch enum value as key too.
 * @author kaeserst
 *
 */
public enum CardFamily {
	HERZ, SCHAUFEL, KREUZ, ECKEN
}
